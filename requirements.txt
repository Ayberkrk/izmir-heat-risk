# İzmir Kentsel Isı Adası Analizi - Gerekli Kütüphaneler
# Kurulum: pip install -r requirements.txt
# Python 3.12 ile test edilmiştir.

# Üst sınırlar bir sonraki major sürümü dışarıda bırakır - rasterio/
# geopandas gibi GDAL tabanlı paketler major geçişlerde sık kırıcı
# değişiklik yapıyor (bkz. issue: "reproducible pipeline" iddiası >= ile
# sınırsız sürüm aralığıyla çelişiyor). Tam bitwise-eş bir ortam için
# constraints.txt kullanın (bkz. "Kurulum ve çalıştırma").

# Uydu verisi çekme (Microsoft Planetary Computer üzerinden)
pystac-client>=0.7,<1.0
planetary-computer>=1.0,<2.0

# Raster (piksel bazlı görüntü) okuma/yazma
rasterio>=1.3,<2.0

# Matematiksel işlemler (dizi/matris hesapları)
numpy>=1.26,<3.0

# Vektör (nokta, çizgi, poligon) işleme
geopandas>=0.14,<2.0
shapely>=2.0,<3.0

# CRS/EPSG doğrulama (city_config.py, validate_config_dict) - geopandas/
# rasterio üzerinden zaten transitive bağımlılık, doğrudan import edildiği
# için burada da açıkça listeleniyor.
pyproj>=3.5,<4.0

# Raster üzerinde istatistik hesaplama (spatial join)
rasterstats>=0.19,<1.0

# İnteraktif harita oluşturma
folium>=0.16,<1.0

# HTTP istekleri (API'ye bağlanmak için)
requests>=2.31,<3.0

# Veri tablosu işlemleri
pandas>=2.0,<4.0

# Jenks doğal kırılım sınıflandırması (HVI kategorileri için)
jenkspy>=0.4,<1.0

# Şehir config.yaml dosyalarını okumak için
pyyaml>=6.0,<7.0
